ansible_connection: winrm
ansible_winrm_transport: ntlm
ansible_winrm_server_cert_validation: ignore
ansible_port: 5985

ansible_become: false

ansible_user: "{{ ad_bootstrap_admin_user }}"
ansible_password: "{{ ad_bootstrap_admin_password }}"
